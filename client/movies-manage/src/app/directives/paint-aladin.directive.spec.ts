import { PaintAladinDirective } from './paint-aladin.directive';

describe('PaintAladinDirective', () => {
  it('should create an instance', () => {
    const directive = new PaintAladinDirective();
    expect(directive).toBeTruthy();
  });
});
